import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  typography: {
    body1: {
      fontFamily: "'Titillium Web', 'Avenir'",
      fontSize: 16,
      fontWeight: 400,
    }
  }
})

export default theme;