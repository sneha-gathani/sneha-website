import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  typography: {
    body1: {
      fontFamily: "'Avenir', 'Titillium Web'",
      fontSize: '14px',
      fontWeight: 400,
    }
  }
})

export default theme;